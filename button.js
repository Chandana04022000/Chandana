const button = document.getElementById("mybutton");
const messagediv = document.getElementById("message");

button.addEventListener('click', () => {
    messagediv.textContent = 'LIVELOVELAUGH';
});
